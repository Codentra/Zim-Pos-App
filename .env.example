# Convex deployment URL. Get this from your Convex dashboard after running `npx convex dev`.
# When set, the app will sync local data to Convex when online.
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Production: sync key. In Convex dashboard set CONVEX_SYNC_KEY; set the same value here.
# When set, only requests with this key can sync. Keep the value secret.
# EXPO_PUBLIC_CONVEX_SYNC_KEY=your-secret-sync-key
