/**
 * Theme utilities. Import useColors from ThemeContext for component-level theming.
 * This file can hold theme-related helpers.
 */
import { colors } from "@/constants/theme";

export type ThemeColors = typeof colors.light;
